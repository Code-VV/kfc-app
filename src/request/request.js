//引入import axios from "axios"//配置axiosconst instance = axios.create(    {        baseURL:"http://kumanxuan1.f3322.net:8001",        timeout:5000    })//请求拦截(每次发送请求前都执行一次请求拦截的代码)instance.interceptors.request.use(config=>{    // console.log("我被执行了,准备触发");    //放行    return config //config此次请求的配置信息},err=>{    //返回错误    return Promise.reject(err);})//响应拦截(每一次响应之前都会执行一次这里的代码)instance.interceptors.response.use(res=>{    // console.log("我是响应拦截,我被执行了");    return res.data;   /*  return config */},err=>{    //返回错误    return Promise.reject(err);})//开放export default instance
